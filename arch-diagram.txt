┌─────────────────────────────────────────────────────────────────┐
│                     VS Code Copilot Chat                         │
│                                                                  │
│   "Brief me on RELIANCE"   "Any new signals?"                    │
└──────────────┬──────────────────────────┬───────────────────────┘
               │ tv_briefing_get          │ tv_live_feed_get(since=N)
               ▼                          ▼
       ┌──────────────────────────────────────────┐
       │   MCP Server (stdio, 80 tools)           │
       │   src/tools/ai.js  ◄── NEW               │
       └──────────────┬───────────────────────────┘
                      │ HTTP fetch
                      ▼
       ┌──────────────────────────────────────────┐
       │   Dashboard (localhost:3456)             │
       │   ├── /api/ai/snapshot ◄── NEW           │ ─┐
       │   ├── /api/ai/feed     ◄── NEW           │  │
       │   ├── /api/* (36 existing routes)        │  │ uses
       │   └── pushLiveEvent() ◄── hooked into    │  │ same
       │       broadcastSSE                       │  │ pipeline
       │                                          │  ▼
       │   ┌──────────────────────────────────┐   │ analyzeMarketConditions
       │   │  Quant Pipeline (UNCHANGED)      │◄──┘ buildSelectionContext
       │   │  └── 4700 lines, all logic intact│   autoSelectStrategy
       │   └──────────────────────────────────┘   backtestAndRerank
       │                                          │
       │   liveFeed[] ring (500) ──► .data/live-feed.jsonl (5MB rotate)
       └──────────────┬───────────────────────────┘
                      │ SSE + REST
                      ▼
       ┌──────────────────────────────────────────┐
       │   React UI (port 3456)                   │
       │   ├── Dashboard / Watchlist / Alerts     │ (existing)
       │   ├── History / Pine Lab / Settings      │ (existing)
       │   └── AI Agent ◄── NEW (8th tab)         │
       │       ├── Quote/Regime/Indicator cards   │
       │       ├── Top 5 Strategies               │
       │       ├── Live Feed (refresh button)     │
       │       └── Markdown Prompt + 6 templates  │
       └──────────────────────────────────────────┘
                      │
                      ▼
                CDP localhost:9222 ──► TradingView Desktop